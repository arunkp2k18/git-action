name: First WorkFlow
on: workflow_dispatch
jobs:
 first-job:
  runs-on: ubuntu-latest
  steps:
   - name: First Step
     run: echo "First Step"
  - name: Second Step
    run: echo "Second Step"
    
